pipeline {
    agent any

    stages {
        stage('Build') {
            steps{
                echo 'Building R3APP'
            }
        }
        stage('Test') {
            steps{
                echo 'Testing R3APP'
            }
        }
        stage('Deploy') {
            steps{
                echo 'Deploying R3APP'
            }
        }
    }
}