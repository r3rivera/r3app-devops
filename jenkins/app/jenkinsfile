def performDeploy(){

}

pipeline {
    agent any

    stages {
        stage('Build') {
            steps{
                echo 'Building R3APP'
                sh "echo ${WORKSPACE}"
            }
        }
        stage('DEV-Deploy') {
            steps{
                echo 'Testing DEV R3APP'
     

            }
        }
        stage('Test Env') {
            steps{
                echo 'Testing TEST R3APP'
            }
        }
        stage('ACPT Env') {
            steps{
                echo 'Deploying ACPT R3APP'
            }
        }
        
    }
}