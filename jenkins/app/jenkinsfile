def performDeploy(){

}

pipeline {
    agent any

    stages {
        stage('Build') {
            steps{
                echo 'Building R3APP'
                sh "dos2unix ../${R3App_Devtools}/deploy/deploy-app.sh"
                sh "../${R3App_Devtools}/deploy/deploy-app.sh"

            }
        }
        stage('DEV-Deploy') {
            steps{
                echo 'Testing DEV R3APP'
     

            }
        }
        stage('Test Env') {
            steps{
                echo 'Testing TEST R3APP'
            }
        }
        stage('ACPT Env') {
            steps{
                echo 'Deploying ACPT R3APP'
            }
        }
        
    }
}