def performDeploy(){
    
}

pipeline {
    agent any

    stages {
        stage('Build') {
            steps{
                echo 'Building R3APP'
                echo "CURRENT WORKSPACE ::: ${WORKSPACE}"

            }
        }
        stage('DEV Deploy') {
            steps{
                echo 'Testing DEV R3APP'
                sh "sudo ./deploy/deploy-app.sh DEV"
            }
        }
        stage('TEST Deploy') {
            steps{
                echo 'Testing TEST R3APP'
            }
        }
        stage('ACPT Deploy') {
            steps{
                echo 'Deploying ACPT R3APP'
            }
        }
        
    }
}